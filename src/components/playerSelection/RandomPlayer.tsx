import romanEmperors from "../../data"

export default function RandomPlayer(){

    function handleRandom(){
        // placeholder
        
    }

    return(
        <div>
            {romanEmperors.map(
                ruler => (
                    <>

                    </>
                )
            )}
        </div>
    )
}